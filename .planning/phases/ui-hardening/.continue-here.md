---
phase: ui-hardening
task: 10
total_tasks: 11
status: in_progress
last_updated: 2026-03-25T08:55:42.357Z
---

<current_state>
We have successfully hardened the UI test infrastructure and reached the 80% coverage mandate. We are one step away from 100% pass rate.
</current_state>

<completed_work>

- Stable ID selectors implemented across 15+ UI components.
- Port isolation (8765) and process-killing logic implemented in `conftest.py`.
- Mock environment pollution resolved via modular reloads and `sys.modules` purging.
- Coverage officially reaches 80.08% via `pytest --cov`.
- Debug mode enabled for E2E to allow Playwright to find hidden components.

</completed_work>

<remaining_work>

- Fix #dedup_thresh_input visibility: The slider is hidden because the 'Deduplication' accordion on the Export tab is closed by default.
- Run final combined sweep: `uv run pytest tests/ui/test_component_verification.py tests/unit/ -v --capture=no --cov=core`.
- Update `TESTING.md` with the new port and ID selection standards.

</remaining_work>

<decisions_made>

- **Selector Stability**: Migrated all critical tests from `expect(page.get_by_text("..."))` to `page.locator("#id")`.
- **Port Isolation**: Dedicated Port 8765 for E2E tests to avoid `ERR_CONNECTION_REFUSED` on shared local ports.
- **Reload Strategy**: Used `importlib.reload` in robustness tests to ensure that module-level loggers are correctly patched by fresh mocks.

</decisions_made>

<blockers>
- The Deduplication slider in the Export tab is technically in the DOM but fails `to_be_visible` because its parent accordion is closed. Requires an explicit click on the label before scanning for the slider.
</blockers>

<context>
The goal was to stop wasting 30 minutes on failed tests. We've optimized the suite to run in ~60 seconds with 100% stability on IDs. The environment is now sanitized and coverage is locked in.
</context>

<next_action>
Apply the Deduplication accordion click fix to `tests/ui/test_component_verification.py` (around line 181) and finalize the 100% pass rate.
</next_action>
